/**
 * @file
 *
 * @brief Atmel vendor redirect
 *
 * Used for the TinyUSB code to stay unmodified, but still be able to include the Atmel vendor
 * headers at `sam.h` in our directory structure.
 */
#include <vendor/sam.h>
